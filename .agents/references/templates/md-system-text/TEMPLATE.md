---
name: md-system-text
description: При работе с составом разделов системного (AI) документа, порядком разделов, эталоном раздела `## Правила`, оформлением пояснений `{...}`, проверкой системного документа перед выдачей.
owner: .agents/rules/md-system-text/RULE.md
---

## Правила

После открытия текущего документа — прочитай и постоянно применяй при работе с целевой сущностью! Если дальнейшими инструкциями или пользователем не указано иное.

- Текущий `## Правила` наследует все `## Правила` в `AGENTS.md` уровнем выше в папочной иерархии текущего проекта.
- Любое несоответствие целевой сущности текущего документа, разделам текущего документа и (если есть) разделам унаследованным текущим документом — не исправлять. Явно сообщить!
- Спорные случаи не решать самому. Явно сообщить! Предложить варианты решения.
- Перед выдачей проверять соответствие разделам текущего документа и (если есть) разделам унаследованным текущим документом.
- Эталон задаёт разделы, специфичные для системного (AI) документа.
- Эталоны:
	- Раздел внутри блока эталона — раздел будущего документа.
	- **Не:**
		- Считать содержание примеров—эталонов разделами этого файла.
		- Проверять состав разделов этого файла по содержанию примеров—эталонов.
- **Не:**
	- Дублировать унаследованные правила в текущем разделе `## Правила`.
- **Исключение:**
	- Заданные эталоном `## Правила` дублировать допускается.

---

## Эталоны

Системный (AI) документ:

```markdown
## Условия

{Список отвечает на вопросы `Что соблюсти до применения раздела ## Правила?`, `Что проверить перед работой?`, `Что требуется на входе: данные, доступы, состояние документа?`.}
{Замена. Непосредственно условия.}

---

## Правила

После открытия текущего документа — прочитай и постоянно применяй при работе с целевой сущностью! Если дальнейшими инструкциями или пользователем не указано иное.

{Список отвечает на вопросы `Кто именно что делает?`, `Как понять, что выполнено?`, `Что будет, если не выполнено?`. Помогающие вопросы: `Чей приоритет при конфликте с другим правилом?`, `Что пересматривается первым при изменении?`.}
- Текущий `## Правила` наследует все `## Правила` в `AGENTS.md` уровнем выше в папочной иерархии текущего проекта.
- Любое несоответствие целевой сущности текущего документа, разделам текущего документа и (если есть) разделам унаследованным текущим документом — не исправлять. Явно сообщить!
- Спорные случаи не решать самому. Явно сообщить! Предложить варианты решения.
- Перед выдачей проверять соответствие разделам текущего документа и (если есть) разделам унаследованным текущим документом.
- {Замена. Дополнительные пункты правил. Если требуются. По аналогии с эталоном.}
- **Не:**
	- Дублировать унаследованные правила в текущем разделе `## Правила`.
	- {Замена. Дополнительные `**Не:**` пункты. Если требуются. По аналогии с эталоном.}
- **Исключение:**
	- Заданные эталоном `## Правила` дублировать допускается.
	- {Замена. Дополнительные `**Исключение:**` пункты. Если требуются. По аналогии с эталоном.}

---

## Инструкции

{Каждая инструкция — отдельный подраздел H5. В списках выше вместо инструкции ставится пункт с отсылкой на её подраздел. Раздел не добавляется, если инструкций в документе нет.}
{Замена. Непосредственно подразделы.}

---

## Связанные документы

После открытия текущего документа — перейди по каждой ссылке, прочитай и постоянно применяй к целевой сущности! Если дальнейшими инструкциями или пользователем не указано иное.

{Список отвечает на вопросы `К чему применятся шаблон?`, `По каким путям искать документы-шаблоны?`. Строка на каждую сущность: путь — роль. Путь от корня репозитория.}
{Замена. Непосредственно список.}

---

## Архитектура

{Список отвечает на вопросы `Что где лежит?`, `Что за что отвечает?`, `Куда класть новое?`. Строка на каждую сущность: путь — роль. Помогающие вопросы: `Что нельзя переносить и почему?`, `Что ломается при переименовании?`, `Какое имя обязательно, какое свободно?`.}
{Замена. Непосредственно список.}

---

## Эталоны

{Список отвечает на вопросы `Какую сущность или вариацию покрывает шаблон?`.}
{Замена. Непосредственно список.}

---

## Примеры: плохо

{Отвечает на вопрос `Как выглядит нарушение правил?`. Подпись на каждый пример, под ней блок с неправильным оформлением.}
{Замена. Непосредственно примеры.}

---

## Примеры: хорошо

{Отвечает на вопрос `Как выглядит правильное оформление?`. Подпись на каждый пример, под ней блок с правильным оформлением. Каждый пример — пара к примеру из раздела `## Примеры: плохо`.}
{Замена. Непосредственно примеры.}

---

## {Замена. Дополнительные H2 блоки. Если требуются. По аналогии с эталоном.}
```

---

## Примеры: плохо

Состав и порядок разделов:

```markdown
## Назначение

## Правила

## Содержание

## Условия
```

Разделы, запрещённые в системном (AI) документе:

```markdown
## Содержание

- [[#Правила]]

---

## Назначение

- Даёт процедуру создания сущности.
```

---

## Примеры: хорошо

Состав и порядок разделов:

```markdown
## Условия

## Правила

## Связанные документы

## Архитектура
```

Системный (AI) документ без общих разделов:

```markdown
## Правила

После открытия текущего документа — прочитай и постоянно применяй при работе с целевой сущностью! Если дальнейшими инструкциями или пользователем не указано иное.

- Любое несоответствие целевой сущности текущего документа, разделам текущего документа и (если есть) разделам унаследованным текущим документом — не исправлять. Явно сообщить!
```
