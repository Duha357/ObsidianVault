---
name: ai-output
description: При работе с правилом оформления ответов, ответом пользователю, оформлением списка, заголовками документа, негативными и исключающими пунктами, предупреждением о деструктивной операции, сомнением в оформлении ответа или документа, двумя трактовками формулировки правила.
---

## Содержание

- [[#Содержание]]
- [[#Назначение]]
- [[#Правила]]
- [[#Форматы]]
- [[#Пример: плохо]]
- [[#Пример: хорошо]]

---

## Назначение

- Показывает применение правила `ai-output` парами: неправильное оформление против правильного.
- На выходе — образец, с которым сверяется спорное оформление.
- Снимает спор о трактовке: сверка идёт по образцу, а не по формулировке правила.
- Ломается на случае, которого в образцах нет: сверять не с чем.
- **Не:**
	- Перечисляет все возможные случаи.

[[#Содержание|↑ Назад]]

---

## Правила

Прочитать и применять сразу. Если дальнейшими инструкциями или пользователем не указано иное.

- Текущий раздел `## Правила` наследует все `## Правила` в `AGENTS.md` уровнем выше в текущем проекте.
- Любое несоответствие правилам — не исправлять. Явно сообщить!
- Спорные случаи не решать самому. Явно сообщить!
- Перед выдачей проверять соответствие `## Правила`, `## Форматы`.
- Сверять оформление с ближайшим образцом, при расхождении образца и правила — с правилом.
- Нормы брать в `.agents/rules/ai-output/RULE.md`: образец правил не создаёт.
- Держать образцы парами: плохо и хорошо на один случай.
- Добавлять новый случай парой, не одиночным примером.
- **Не:**
	- Дублировать унаследованные правила в текущем разделе `## Правила`.
- **Исключение:**
	- Заданные эталоном `## Правила` дублировать допускается.

[[#Содержание|↑ Назад]]

---

## Форматы

Прочитать и применять сразу. Если дальнейшими инструкциями или пользователем не указано иное.

- `.md` документов:
	- `.agents/rules/md-doc/RULE.md`
- Текстов:
	- `.agents/rules/ai-output/RULE.md`

[[#Содержание|↑ Назад]]

---

## Пример: плохо

Обычный текст:

```markdown
Конечно! Я с удовольствием вам помогу. Проблема, с которой вы столкнулись, скорее всего, вызвана...
```

Список:

```markdown
Итак, я внимательно проверил реализацию по плану. Стоит отметить, что в целом большинство пунктов выполнено, однако при детальном рассмотрении выяснилось, что пункт 3, касающийся валидации, реализован лишь частично, поскольку проверка длины строки присутствует, а вот проверка формата email, как видим, отсутствует. Надеюсь, это поможет вам разобраться.
```

Негативные и исключающие пункты:

```markdown
- Не нажимать красную кнопку.
- Если пользователь не указал явно.
- Не чихать.
- Без использования автоприцела.
- Только не когда пользователь чихнул 2 раза.
```

[[#Содержание|↑ Назад]]

---

## Пример: хорошо

Обычный текст:

```markdown
Ошибка в промежуточном ПО аутентификации.
```

Список:

```markdown
## Находки

### Пункт 3 (валидация)

- Реализован частично.
- Проверка длины есть.
- Проверки формата email нет — `src/validators/user.ts:42`.

### Пункт 5

- Чекбокс закрыт. Записи в Progress Log нет.

### Scope creep

- Добавлен эндпоинт `/api/debug`. В плане нет.
```

Негативные и исключающие пункты:

```markdown
- **Не:**
	- Нажимать красную кнопку.
	- Чихать.
	- Использовать автоприцел.
- **Исключение:**
	- Явное указание пользователя.
	- Пользователь чихнул 2 раза.
```

Деструктивная операция:

```markdown
Внимание: это приведёт к безвозвратному удалению всех строк в таблице users; действие необратимо.

`DROP TABLE users;`

Сначала убедитесь, что резервная копия существует.
```

[[#Содержание|↑ Назад]]
