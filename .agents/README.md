# .agents

Кросс-агентные материалы проекта: правила оформления (`rules`), процедуры (`skills`), справочники (`references`). Работают одинаково для ИИ-агентов и людей.

Раскладка папок, именование файлов и порядок создания новой сущности — `.agents/AGENTS.md`.
